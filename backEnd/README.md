# Revive Backend (Hackathon MVP)

Contract source of truth:

- `frontend-backend-contract.md`
- `cahierChargev1.md`

## Run

```powershell
cd C:\Users\DELL\Desktop\revive\backEnd
npm install
npm run dev
```

Default dev port: `3001`

## Environment

Gemini is optional and only used to polish phrasing. Deterministic scoring still selects the route.

- `GEMINI_API_KEY`
- `GEMINI_MODEL` (optional)
- `GEMINI_API_BASE_URL` (optional)

